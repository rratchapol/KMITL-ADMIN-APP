import { Route } from '@angular/router';
import { ConfignotiComponent } from './confignoti.component';

export const ConfignotiRoute: Route[] = [
    {
        path: '',
        component: ConfignotiComponent,
    },

];
